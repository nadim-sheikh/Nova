import Foundation

/// Who built Nova, shown in the About panel and Settings.
enum DeveloperCredits {
    static let developerName = "Nadim"
    static let xHandle = "@Nadim_404"
    static let xProfileURL = URL(string: "https://x.com/Nadim_404")
}
